# Echo 模型 SHA256 校验清单（R-4.2 可复现交付）
#
# 格式：<sha256>  <filename>
#
# 本清单由 `bash Scripts/prepare_models.sh --generate-checksums` 生成。
# 首次下载模型后运行该命令填充真实校验值；之后 `prepare_models.sh`
# 在 download/verify 模式下会比对下载文件的 SHA256，不匹配即失败。
#
# ⚠️ 提交前必须用真实模型的 SHA256 填充本文件（当前为占位结构）。
# 生成方式：
#   1. bash Scripts/prepare_models.sh --generate-checksums
#   2. 检查本文件已填入 64 位十六进制校验值
#   3. git add Scripts/model_checksums.sha256 && git commit
#
# 期望条目（filename 必须与 prepare_models.sh 中一致）：
#   Manifest.json              (multilingual-e5-small mlpackage 清单)
#   whisper-small-q4_k.gguf    (Whisper ASR GGUF)
#   model.safetensors          (SigLIP2-B/32 checkpoint)
