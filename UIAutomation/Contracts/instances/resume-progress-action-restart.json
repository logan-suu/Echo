{
  "$schema": "https://echo-ui-automation/schemas/contracts/action/v1",
  "actionId": "prompt.restart",
  "source": "user",
  "targetIntent": "ResumeProgressViewModel.restartTask()",
  "expectedOutcome": "A registered allow-list launcher validates exact taskId, descriptor and current authorization; ProgressActor transactionally replaces that checkpoint with an index-0 retryable record, then queueing occurs as a separate observable phase. Queue failure retains the reset record and cannot report restarted; only queue acceptance publishes restarted (US-SYS-001 AC-4; AGENTS.md §4.5)",
  "requiresConfirmation": false,
  "version": "1.2.0"
}
