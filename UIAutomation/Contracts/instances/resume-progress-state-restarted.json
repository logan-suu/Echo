{
  "$schema": "https://echo-ui-automation/schemas/contracts/state/v1",
  "stateId": "restarted",
  "surfaceId": "resume-progress-prompt",
  "dataConditions": {
    "userDecision": "restart (prompt.restart)",
    "resumeTarget": "same taskId checkpoint was transactionally replaced with retryable index 0 and the queue accepted the reconstructed job"
  },
  "allowedActions": [],
  "expectedSemantics": {
    "dialogDismissed": "Prompt closes",
    "outcome": "Queue accepted the validated fresh job; checkpoint replacement alone is not restarted success"
  },
  "version": "1.1.0"
}
