{
  "$schema": "https://echo-ui-automation/schemas/contracts/state/v1",
  "stateId": "error",
  "surfaceId": "resume-progress-prompt",
  "dataConditions": {
    "pendingProgress": "unavailable because ProgressActor load failed or no valid production task launcher exists — L2",
    "isError": true
  },
  "allowedActions": ["error.retry"],
  "expectedSemantics": {
    "errorMessage": "Unable to check or reconstruct the saved task (L2); no resumed/restarted success is shown",
    "retryButton": "Retry re-runs the progress check and launcher resolution",
    "readingOrder": ["errorMessage", "retryButton"]
  },
  "version": "1.1.0"
}
