{
  "$schema": "https://echo-ui-automation/schemas/contracts/action/v1",
  "actionId": "detail.keepLocal",
  "source": "user",
  "targetIntent": "MemoryDetailViewModel.resolveConflict(keep: .local)",
  "expectedOutcome": "Persist the local edit, clear MemoryEditConflict and set userLocked=true so automatic sync skips the memory until explicit resync or external restore (US-AWK-007 AC-4, ADR-017).",
  "requiresConfirmation": false,
  "version": "1.2.0"
}
