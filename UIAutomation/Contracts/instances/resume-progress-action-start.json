{
  "$schema": "https://echo-ui-automation/schemas/contracts/action/v1",
  "actionId": "task.start",
  "source": "system",
  "targetIntent": "ResumeProgressViewModel.checkForPendingProgress(taskType:)",
  "expectedOutcome": "Host task start asks the recovery coordinator for orphaned records, excluding current-session queue-owned taskIds; every exact matching taskId remains visible, and raw unknown types transition to retained L2 instead of disappearing",
  "requiresConfirmation": false,
  "version": "1.1.0"
}
