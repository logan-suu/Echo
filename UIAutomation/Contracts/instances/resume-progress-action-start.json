{
  "$schema": "https://echo-ui-automation/schemas/contracts/action/v1",
  "actionId": "task.start",
  "source": "system",
  "targetIntent": "ResumeProgressViewModel.checkForPendingProgress(taskType:)",
  "expectedOutcome": "Host task start triggers a ProgressActor pending-progress check; transitions to prompt (pending found) / none (no pending) / error (L2)",
  "requiresConfirmation": false,
  "version": "1.0.0"
}
