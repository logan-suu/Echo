{
  "$schema": "https://echo-ui-automation/schemas/contracts/action/v1",
  "actionId": "prompt.continue",
  "source": "user",
  "targetIntent": "ResumeProgressViewModel.continueTask()",
  "expectedOutcome": "A registered allow-list launcher validates exact taskId, raw taskType, bounded versioned resumeData and current authorization, then re-enqueues without overwriting lastProcessedIndex; only queue acceptance publishes resumed, while any pre-enqueue failure retains the record and shows L2 (US-SYS-001 AC-3)",
  "requiresConfirmation": false,
  "version": "1.2.0"
}
