# Echo · 回响：数据流全链路技术说明文档

**版本**：v1.1

**生效日期**：2026-07-04

**适用架构**：Cognitive Pipeline + Observable ViewModel + Actor Isolation

**对应规格**：Echo v4.6 全量用户故事与验收标准规格书

本文档详细描述数据在 Echo 各组件间的流动路径，涵盖用户交互、后台任务、错误处理、断点续传等全链路行为。所有流程均遵循 **本地优先、隐私可审计、任务可中断/续传** 的原则。

------

## 1. 整体数据流架构

```mermaid
flowchart TB
    subgraph User["🎨 用户交互层"]
        UI["SwiftUI Views"]
        VM["@Observable ViewModels<br/>MainActor"]
    end

    subgraph Cognitive["🧠 认知管线层"]
        SP["SearchPipeline"]
        IP["IngestPipeline"]
        SYP["SyncPipeline"]
        AP["AwakeningPipeline"]
        FP["FeedbackPipeline"]
    end

    subgraph Actors["🛡️ Actor 隔离服务层 (Swift 6)"]
        PA["PrivacyActor"]
        EA["ExcludedAssetsActor"]
        FA["FeedbackActor"]
        PR["ProgressActor"]
        PO["PendingOpsActor"]
        TQ["TaskQueueActor"]
        SM["SystemMonitor Actor"]
    end

    subgraph Storage["💾 存储层"]
        VectorStoreActor[("VectorStoreActor<br/>(ProximaKit HNSW)")]
        SQL[("SQLite<br/>排除表/反馈/进度/待处理/审计")]
        FileRef["系统相册/备忘录引用"]
    end

    subgraph External["🔌 外部系统 (Read-only)"]
        PhotoKit["PhotoKit"]
        EventKit["EventKit"]
        HealthKit["HealthKit"]
        NoteStore["NoteStore"]
    end
    
    SystemMonitor["⚙️ 系统状态<br/>电量/热/定位"]

    UI --> VM
    VM -->|"调用"| SP & IP & SYP & AP & FP
    SP & IP & SYP & AP & FP -->|"读写"| Actors
    Actors -->|"ACID & Vector I/O"| Storage
    Actors -->|"System API"| External
    SM -.->|"监听"| SystemMonitor
    SystemMonitor -->|"通知降级"| TQ

    %% 样式美化
    classDef ui fill:#e1f5fe,stroke:#03a9f4,stroke-width:2px;
    classDef pipeline fill:#f3e5f5,stroke:#9c27b0,stroke-width:2px;
    classDef actor fill:#fff3e0,stroke:#ff9800,stroke-width:2px;
    classDef storage fill:#e8f5e9,stroke:#4caf50,stroke-width:2px;
    classDef external fill:#eceff1,stroke:#607d8b,stroke-width:2px,stroke-dasharray: 5 5;
    classDef system fill:#ffebee,stroke:#e53935,stroke-width:2px,stroke-dasharray: 5 5;

    class User ui;
    class Cognitive pipeline;
    class Actors actor;
    class Storage storage;
    class External external;
    class SystemMonitor system;
```

**数据流特点**：

- **单向数据流**：UI → ViewModel → Pipeline → Actor → 存储。结果通过 `AsyncStream` 反向传递至 ViewModel，驱动 UI 更新。
- **Actor 隔离**：所有可变状态（数据库、队列、排除表）由 Actor 独占，避免数据竞争。
- **串行化保证**：`TaskQueueActor` 确保索引构建与数据同步互斥执行。

------

## 2. 用户发起检索数据流（SearchPipeline）

### 2.1 流程时序图

```mermaid
sequenceDiagram
    participant View as SearchView
    participant VM as SearchViewModel<br/>(@MainActor)
    participant SP as SearchPipeline
    participant PA as PrivacyActor
    participant FA as FeedbackActor
    participant VA as VectorStoreActor
    participant Audit as AuditLog (SQLite)

    View->>VM: 用户输入查询 "开心的事"
    VM->>SP: search(query: "开心的事")
    SP->>PA: validate(operation: .search)
    PA->>PA: 检查 UserPolicy 授权
    PA-->>SP: PrivacyCheckpoint (含 traceID)
    SP->>Audit: 写入审计记录 (.retrieval)
    SP->>FA: computeAdjustment(for: memories, query:)
    FA->>FA: 从 FeedbackStore 读取反馈<br/>筛选 cosine ≥0.80 的记录<br/>计算衰减系数并求和，截断至 [-0.5,0.5]
    FA-->>SP: adjustment = +0.3
    SP->>VA: 执行 ANN 检索 + 元数据过滤<br/>(时间/标签/人物/地点)
    VA-->>SP: 候选记忆列表 (原始相似度)
    SP->>SP: 应用重排: finalScore = cosineSim + adjustment
    SP->>SP: 低置信度标记 (<0.6 且跨语言匹配)
    SP-->>VM: 返回 SearchResult (含锚点、置信度)
    VM->>View: 更新 @Observable 属性，渲染列表
```

### 2.2 关键节点说明

| 节点            | 职责                                               | 错误处理                                       |
| --------------- | -------------------------------------------------- | ---------------------------------------------- |
| `PrivacyActor`  | 验证当前操作是否违反用户授权（如检索排除的数据源） | 若拒绝，直接返回 `.denied` 空结果，审计标记    |
| `FeedbackActor` | 读取所有反馈记录，计算调整值                       | 读取失败时返回 0，记录 L2 错误至 `PendingOps`  |
| `VectorStoreActor` | 向量检索 + 标量过滤                                | 超时 (2s) 返回部分结果，标记 `.partialResults` |
| **输出**        | 包含记忆 ID、原始文本/媒体、溯源锚点、置信度提示   | 低置信度时 UI 显示预设文案                     |

------

## 3. 数据源接入与记忆摄入数据流（IngestPipeline）

### 3.1 流程时序图（以图片为例）

```mermaid
sequenceDiagram
    participant PhotoKit as PHPhotoLibrary
    participant IP as IngestPipeline
    participant EA as ExcludedAssetsActor
    participant TQ as TaskQueueActor
    participant PR as ProgressActor
    participant Model as ModelLoader (Core ML)
    participant VA as VectorStoreActor
    participant Audit as AuditLog

    PhotoKit->>IP: 新照片通知 (BGAppRefreshTask)
    IP->>TQ: enqueue(task: .ingestPhoto(assetId))
    TQ->>PR: save(taskId, progress=0)
    loop 每张待处理照片
        IP->>EA: contains(assetId:)
        alt 已排除
            IP->>PR: 跳过该资产，更新进度
        else 未排除
            IP->>Model: 加载 SigLIP 模型 (若未加载)
            Model-->>IP: 向量 (768d)
            IP->>VA: 开始事务
            IP->>VA: 写入向量 + 元数据 (EXIF, 时间戳, 人物标签)
            IP->>VA: 提交事务
            alt 写入失败
                VA-->>IP: 回滚
                IP->>IP: 错误升级为 L2，写入 PendingOperations
            end
        end
    end
    IP->>PR: 更新进度 (processedCount/totalCount)
    IP->>PR: delete(taskId) (完成后清理)
    IP->>Audit: 记录 .imageIngested (含 privacyBlurApplied=false)
    IP->>TQ: 任务完成，释放队列
```

### 3.2 特殊流程：视频摄入

视频摄入分为画面关键帧 + 音频轨道两步：

1. **关键帧向量化**：每秒 ≤2 帧，每帧同图片流程。
2. **音频转写**：调用 `ASREngine` (Whisper.cpp) 转写文本，然后走文本记忆摄入流程（与备忘录相同）。
3. **关联**：画面向量与音频文本向量通过 `memoryGroupId` 关联。

### 3.3 错误与重试

- **L1 瞬态**（如 PHPhotoLibrary 繁忙）：指数退避重试 3 次。
- **L2 可恢复**（如磁盘不足）：写入 `PendingOperations` 表，等待用户手动重试。
- **模型加载失败**：`ModelLoader` 抛出 L3 阻断，停止摄入并引导用户修复。

------

## 4. 数据源变更自动同步数据流（SyncPipeline）

### 4.1 触发方式

| 数据源           | 检测方式                                                     | 降级策略                         |
| ---------------- | ------------------------------------------------------------ | -------------------------------- |
| 相册 (照片/视频) | `PHPhotoLibraryChangeObserver`                               | 无降级                           |
| 备忘录           | 前台对比 `lastUsedDate` + 哈希（若 >100KB 或内存低则仅对比时间戳+大小） | 哈希跳过时标记 `.hasHashSkipped` |
| 日历             | 前台对比 `lastModified` 时间戳，`EKEventStoreChangedNotification` 仅辅助 | 无                               |

### 4.2 流程时序图

```mermaid
sequenceDiagram
    participant System as iOS 系统通知
    participant SYP as SyncPipeline
    participant TQ as TaskQueueActor
    participant PR as ProgressActor
    participant EA as ExcludedAssetsActor
    participant VA as VectorStoreActor
    participant IP as IngestPipeline (复用)

    System->>SYP: 变更通知 (assetId: old, new)
    SYP->>TQ: enqueue(task: .sync(assetId))
    TQ->>PR: save(taskId, progress)
    SYP->>EA: contains(assetId:)
    alt 已排除
        SYP->>PR: 跳过
    else 未排除
        SYP->>VA: 开启事务
        SYP->>VA: 删除旧记忆 (向量+元数据)
        SYP->>IP: 重新摄入新内容 (调用 IngestPipeline)
        IP-->>SYP: 新记忆 ID
        SYP->>VA: 提交事务
        SYP->>PR: updateProgress
    end
    SYP->>PR: 完成任务，删除进度记录
```

### 4.3 冲突处理（L4）

当后台同步过程中用户正在手动编辑同一条记忆时，`SyncPipeline` 会收到 `userLocked` 标记：

- 阻止删除操作，将该记忆标记为 `conflict`。
- 保留用户编辑的副本。
- 在用户下次打开编辑时弹窗，提供合并界面。

------

## 5. 主动唤醒数据流（AwakeningPipeline）

### 5.1 地理围栏唤醒

```mermaid
sequenceDiagram
    participant CLLocation as CoreLocation
    participant AP as AwakeningPipeline
    participant PA as PrivacyActor
    participant SP as SearchPipeline
    participant TQ as TaskQueueActor

    CLLocation->>AP: didEnterRegion(围栏ID)
    AP->>AP: 检查上次推送时间 (存储于 UserDefaults)
    alt 同一围栏24h内已推送且未离开
        AP->>AP: 忽略
    else 可推送
        AP->>PA: validate(operation: .awakening)
        PA-->>AP: 通过
        AP->>SP: search(query: "位置关联记忆", geoFilter)
        SP-->>AP: 召回记忆 (匹配度≥0.7)
        AP->>TQ: enqueue(task: .generateCard)
        TQ-->>AP: 卡片内容 (文案+照片+离线音乐)
        AP-->>Notification: 发送本地推送
        AP->>AP: 记录推送时间
    end
```

### 5.2 情绪唤醒（基于 HealthKit + 文本情感）

```mermaid
flowchart LR
    A[HealthKit 心率变异性] -->|有数据| B[推断情绪状态]
    C[查询字符串+感受] -->|无 HealthKit| D[情感分析模型<br/>防抖 30s]
    D --> B
    B --> E{情绪状态}
    E -->|negative| F[检索 positiveEmotion 标签]
    E -->|neutral| G[检索 reflective 标签]
    E -->|positive| H[不推送]
    F & G --> I[生成回忆卡片]
    I --> J[本地推送]
```

### 5.3 音乐推荐逻辑

- **优先**：Apple Music API（若授权）推荐基于记忆时间/地点/标签的歌单。
- **备选**：内置离线热门歌曲库（按年份分类，每年 20 首），随 App 版本更新。
- **无授权**：显示“需要 Apple Music 授权”提示。

------

## 6. 反馈学习数据流（FeedbackPipeline）

### 6.1 点赞/点踩流程

```mermaid
sequenceDiagram
    participant View as MemoryCardView
    participant VM as HomeViewModel
    participant FP as FeedbackPipeline
    participant FA as FeedbackActor
    participant SQL as FeedbackStore (SQLite)

    View->>VM: 用户点击 👍
    VM->>FP: recordFeedback(memoryId, sentiment: .positive, queryEmbedding)
    FP->>FA: 写入反馈记录
    FA->>SQL: INSERT INTO FeedbackStore (memoryId, sentiment, queryHash, timestamp)
    SQL-->>FA: success
    FA-->>FP: 完成
    FP-->>VM: 返回，UI 按钮置灰
    VM->>View: 更新状态
```

### 6.2 重排公式应用（已在 SearchPipeline 中说明）

### 6.3 重置/撤销流程

```mermaid
flowchart TD
    A(["👤 用户进入“我的反馈记录”页面"]) --> B{"🎯 选择操作"}
    
    B -->|"🔄 重置所有"| C["调用 FeedbackActor.reset()"]
    C --> D[("🗑️ 删除 FeedbackStore 所有记录")]
    D --> E["⚖️ 下次检索恢复默认权重"]
    
    B -->|"↩️ 撤销单条"| F["调用 FeedbackActor.revoke(feedbackId)"]
    F --> G[("🗑️ 物理删除该记录")]
    G --> E

    %% 样式美化
    classDef startEnd fill:#e3f2fd,stroke:#1565c0,stroke-width:2px;
    classDef decision fill:#fff3e0,stroke:#e65100,stroke-width:2px;
    classDef action fill:#f3e5f5,stroke:#6a1b9a,stroke-width:2px;
    classDef db fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px;
    classDef result fill:#eceff1,stroke:#455a64,stroke-width:2px,stroke-dasharray: 5 5;

    class A startEnd;
    class B decision;
    class C,F action;
    class D,G db;
    class E result;
```

------

## 7. 统一错误处理与重试流程

### 7.1 错误处理架构

```mermaid
flowchart TD
    A[Pipeline 节点抛出 Error] --> B{ErrorHandler 分级}
    B -->|L1 瞬态| C[指数退避重试<br/>1s,2s,4s 最多3次]
    C -->|成功| D[继续执行]
    C -->|3次后仍失败| E[升级为 L2]
    B -->|L2 可恢复| F[写入 PendingOperations 表]
    F --> G[Toast 提示用户点击重试]
    G --> H[用户点击 → 重新执行]
    B -->|L3 阻断| I[停止相关功能<br/>全屏引导页+跳转系统设置]
    B -->|L4 数据冲突| J[标记 conflict<br/>提供手动合并界面]
```

### 7.2 PendingOperations 表生命周期

| 阶段 | 操作                                                         |
| ---- | ------------------------------------------------------------ |
| 写入 | 当 L2 错误发生时，`PendingOpsActor.add(operation:)` 将操作序列化存入 SQLite（含重试计数、参数快照）。 |
| 重试 | 用户点击 UI 重试按钮 → ViewModel 调用 `PendingOpsActor.retry(operationId:)` → 反序列化参数，重新调用 Pipeline。 |
| 清理 | 重试成功则删除记录；若重试仍失败且达到上限（3次），则升级为 L3 阻断。表最大 1000 条，超出丢弃最旧记录。 |

------

## 8. 断点续传与进度管理流程

### 8.1 任务进度存储

```mermaid
stateDiagram-v2
    [*] --> Idle
    Idle --> Running: TaskQueueActor.enqueue
    Running --> SavingProgress: 定期或每处理N条
    SavingProgress --> ProgressActor.save
    ProgressActor --> SQLite(TaskProgress)
    SQLite --> Running
    Running --> Paused: 用户点击暂停
    Paused --> Running: 用户点击恢复 → 从 SQLite 读取进度
    Running --> Cancelled: 用户点击取消
    Cancelled --> Idle: 保留进度记录，下次询问
    Cancelled --> Running: 用户选择“继续” → 加载进度
    Running --> Completed: 成功
    Completed --> ProgressActor.delete: 删除进度记录
    Completed --> [*]
```

### 8.2 进度推送至 UI

`TaskQueueActor` 维护一个 `AsyncStream<ProgressEvent>`，ViewModel 通过 `for await` 订阅。每个 `ProgressEvent` 包含：

- `taskId`
- `taskType`
- `processedCount`
- `totalCount`
- `status` (running/paused/cancelled/completed)

ViewModel 收到事件后更新 `@Observable var progress: Double`，驱动进度条刷新。

------

## 9. 隐私校验与审计日志流

### 9.1 强制校验点注入

每个 Pipeline 节点的 `execute` 方法第一个语句必须是：

```swift
let checkpoint = await PrivacyActor.shared.validate(operation: .search, traceID: traceID)
```

若未包含，CI 静态扫描会阻断合并。

### 9.2 审计日志写入流程

```mermaid
sequenceDiagram
    participant Pipeline as Pipeline 节点
    participant PA as PrivacyActor
    participant AuditDB as AuditLog SQLite
    participant Cleanup as 后台清理任务

    Pipeline->>PA: validate(operation:)
    PA->>PA: 生成 traceID, 检查授权
    PA->>AuditDB: INSERT INTO AuditLog (eventType, timestamp, policyVersion, decision, ...)
    AuditDB-->>PA: OK
    PA-->>Pipeline: PrivacyCheckpoint
    loop 每天凌晨
        Cleanup->>AuditDB: DELETE WHERE timestamp < (now - 30 days)
    end
```

### 9.3 审计日志字段完整性

所有字段遵循规格书附录 15，包含 `traceID`、`policyVersion`、`sourceType`、`elapsedMs` 等。缺失字段的审计记录会被 CI 拒绝。

------

## 10. 低电量/过热降级流程

### 10.1 系统状态监控

`SystemMonitor Actor` 监听：

- `NSProcessInfoPowerStateDidChangeNotification` → 低电量模式变化
- `ProcessInfoThermalStateDidChangeNotification` → 热状态变化

### 10.2 降级处理流程

```mermaid
flowchart TD
    A[SystemMonitor 检测到低电量/过热] --> B{用户设置?}
    B -->|低电量时自动暂停后台任务 开启| C[通知 TaskQueueActor 暂停所有非用户任务]
    B -->|低电量时自动暂停后台任务 关闭| D[不暂停，仅降低模型精度]
    C --> E[显示警告 Banner]
    D --> E
    E --> F[Embedder 切换至 MobileCLIP-B]
    F --> G[恢复条件满足时]
    G --> H[TaskQueueActor 恢复任务]
    H --> I[恢复 SigLIP，Banner 消失]
```

### 10.3 对用户体验的影响

- 低电量模式下检索精度可能降低，但 UI 会明确提示。
- 用户可随时手动触发后台任务（如手动导入），不受降级影响（尊重用户主动性）。

------

## 11. 全链路数据流总结

| 业务流程     | 入口               | 关键 Actor                                         | 存储依赖                 | 错误处理级别 | 是否支持断点续传 |
| ------------ | ------------------ | -------------------------------------------------- | ------------------------ | ------------ | ---------------- |
| **检索**     | 用户输入           | PrivacyActor, FeedbackActor                        | VectorStoreActor (ProximaKit), FeedbackStore | L1/L2        | 否               |
| **摄入**     | 系统通知/手动      | ExcludedAssetsActor, ProgressActor, TaskQueueActor | VectorStoreActor (ProximaKit), SQLite (排除表) | L1/L2/L3     | 是（长任务）     |
| **同步**     | 系统通知/手动      | ExcludedAssetsActor, ProgressActor, IngestPipeline | VectorStoreActor (ProximaKit), SQLite          | L1/L2/L4     | 是               |
| **唤醒**     | 地理围栏/时间/情绪 | PrivacyActor, SearchPipeline                       | 无持久化（仅推送记录）   | L1           | 否               |
| **反馈**     | 用户点击           | FeedbackActor                                      | FeedbackStore            | L2           | 否               |
| **错误重试** | 用户手动           | PendingOpsActor                                    | PendingOperations 表     | L2           | 不适用           |
| **断点续传** | 任务取消后         | ProgressActor, TaskQueueActor                      | TaskProgress 表          | 不适用       | 是               |

------

## 12. 数据流安全与隐私保障

- **最小化数据暴露**：所有处理均在端侧，无网络上传。原始媒体文件仅引用 PHAsset，不复制。
- **排除表强制执行**：`ExcludedAssetsActor` 在所有导入/同步流程中被前置调用，确保用户排除的内容永不出现。
- **级联删除**：原始文件删除时，`cascadeDeleteFromOriginal` 同时清除向量、索引、排除表记录，并提示用户。
- **审计日志**：所有关键操作（授权变更、记忆删除、模型加载失败等）均有不可篡改的本地审计记录，保留 30 天。

------

**文档维护声明**

本文档与 Echo v4.6 规格书、技术选型 v4.6、架构设计 v1.0 严格对齐。任何数据流变更必须同步更新本文档并评审。

下次复审：2026-08-31。