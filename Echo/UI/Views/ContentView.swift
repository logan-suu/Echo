// ==========================================
// 文件: ContentView.swift
// 对应规格: AGENTS.md §10.1 UI 层结构
// 任务: 1.1 - 创建 Xcode 项目，配置 Swift 6 并发严格模式
// 用途: 主内容视图（占位，后续由任务 3.1 替换）
// 生成时间: 2026-07-04
// ==========================================

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
